#!/bin/bash

# Display header
echo "USER       PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND"

# Use ps command to display all processes in a tree-like format
ps axo user,pid,%cpu,%mem,vsz,rss,tty,stat,start,time,comm --forest
